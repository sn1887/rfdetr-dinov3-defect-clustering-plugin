# Reference dependency set for the plugin-managed environment.
#
# Production CUDA deployments often need an administrator-managed DSS code
# environment so the PyTorch/CUDA/Faiss builds match the execution node. See
# requirements-gpu-notes.md before installing this file unchanged.

hydra-core==1.3.2
omegaconf==2.3.0
packaging==24.2
numpy==2.1.3
pandas==2.2.3
pyarrow==18.1.0
Pillow==11.1.0
faiss-cpu==1.9.0.post1
timm==1.0.20
safetensors==0.5.3
rfdetr==1.5.2
